//! # Implement equality for primitive buckets.
//! Each type should have its own file in which it should:
//! - implement `PartialEq`
//! - implement custom equality logic for differing primitive types.

mod bigint;
mod float;
mod int;
